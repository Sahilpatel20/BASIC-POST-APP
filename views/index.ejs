<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-zinc-900 text-white flex justify-center items-center h-screen">
    <div>
        <h3 class="text-3xl mb-6 text-center">Create User</h3>
        <form action="/register" method="post" class="flex flex-col gap-4">
            <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-700" type="text"
                placeholder="Username" name="username" required>
            <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-700" type="text" placeholder="Name"
                name="name" required>
            <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-700" type="email" placeholder="Email"
                name="email" required>
            <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-700" type="password"
                placeholder="Password" name="password" required>
            <input class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-700" type="number" placeholder="Age"
                name="age" required>
            <button type="submit"
                class="px-5 py-3 rounded-md bg-blue-500 hover:bg-blue-600 transition font-semibold">Create User</button>
        </form>
    </div>
</body>

</html>