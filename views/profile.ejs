<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-[#121212] text-white min-h-screen relative font-sans">
    <!-- Logout -->
    <div class="absolute top-6 right-6">
        <a href="/logout"
            class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-md text-sm font-semibold shadow-md transition-all duration-300">
            Logout
        </a>
    </div>

    <div class="px-8 md:px-20 pt-24">
        <div class="flex items-center gap-4 mb-2">
            <div class="w-10 h-10 rounded-md bg-red-500 overflow-hidden">
                <!-- ✅ Corrected image path -->
                <img class="w-full h-full object-cover" src="/images/<%= user.profilepic %>" alt="Profile Picture">
            </div>

            <!-- Greeting -->
            <h1 class="text-3xl font-semibold mb-2">
                Hello, <span class="font-medium">
                    <%= user.name %>
                </span> 👋
            </h1>
        </div>
        <p class="text-zinc-400 mb-6">You can create a new post.</p>

        <!-- Create Post -->
        <form action="/post" method="post" class="mb-12">
            <textarea name="content" rows="4" placeholder="What's on your mind?"
                class="w-full md:w-1/2 bg-zinc-800 text-white p-4 rounded-lg border border-zinc-700 focus:outline-none focus:ring-2 focus:ring-blue-500 transition duration-300"></textarea>
            <br />
            <button
                class="mt-4 bg-blue-600 hover:bg-blue-700 text-white font-semibold px-5 py-2 rounded-lg shadow-md transition-all duration-300">
                Create New Post
            </button>
        </form>

        <!-- Posts -->
        <h3 class="text-zinc-400 text-lg mb-4">Your Posts.</h3>

        <% user.posts.slice().reverse().forEach(post=> { %>
            <div class="bg-zinc-800 w-full md:w-1/2 p-4 rounded-lg mb-4">
                <h4 class="text-blue-400 font-semibold">@<%= user.username %>
                </h4>
                <p class="text-zinc-300 mt-2">
                    <%= post.content %>
                </p>
                <small>
                    <%= post.likes.length %> likes
                </small>
                <div class="flex items-center gap-4 mt-3 text-sm">
                    <a class="text-blue-400 hover:underline" href="/like/<%= post._id %>">
                        <% if (post.likes.some(like=> like.toString() === user._id.toString())) { %>
                            Unlike
                            <% } else { %>
                                Like
                                <% } %>
                    </a>
                    <a class="text-zinc-400 hover:text-white transition" href="/edit/<%= post._id %>">Edit</a>
                </div>
            </div>
            <% }) %>
    </div>
</body>

</html>